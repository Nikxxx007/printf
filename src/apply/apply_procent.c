#include "../../includes/ft_printf.h"

void	apply_procent(const char *format, t_procent *pr, int *counter, va_list list)
{
	if (pr->precision != -100)

	if (pr->width != -100)	//parse flag inside this func



}
